# Opcional. Si se define, app/api/contact/route.ts puede usarla para enviar
# el email real con Resend (ver el comentario dentro de ese archivo).
# Sin esta variable, el endpoint sigue funcionando y deja el mensaje en el
# log del servidor — no se pierde ningún lead durante desarrollo.
RESEND_API_KEY=
